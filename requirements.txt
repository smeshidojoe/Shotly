# Shotly — runtime dependencies.
#   pip install -r requirements.txt

# GUI.
PySide6==6.11.1

# Запасной путь для горячих клавиш. Основной — системный RegisterHotKey из
# core/hotkey.py, без единого хука клавиатуры; библиотека нужна только когда
# сочетание уже занято другой программой (RegisterHotKey эксклюзивен).
# Программа работает и без неё.
keyboard==0.13.5

# WinAPI: захват экрана через BitBlt и отрисовка курсора в кадр.
pywin32==311

# Связка корневых сертификатов для наших HTTPS-запросов (проверка обновлений).
# В хранилище Windows бывает просроченная цепочка Let's Encrypt, и тогда запрос
# падает на проверке — certifi это закрывает (см. core/updater.ssl_context).
certifi==2026.5.20

# Сборка assets/app.ico из отрисованных кадров (tools/make_assets.py).
# В рантайме не нужен.
pillow==12.1.0

# --- Build only ------------------------------------------------------------ #
# pyinstaller==6.11.1
